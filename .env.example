# SQLite works without configuration. Override with PostgreSQL when required.
DATABASE_URL=sqlite:///data/stocks.db
# Direct CafeF export URL or local CSV/ZIP path. No credentials are required.
CAFEF_DATASET_URL=
CONFIG_FILE=config/config.yaml
